export const metadata = {
  title: "Yvainely Order Form",
  description: "Yvainely Fleur Order Form",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
